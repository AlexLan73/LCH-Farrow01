# 🚀 LCH-Farrow01: Упрощенная конфигурация для вашей системы

## ✅ Ваша текущая система:

```
📍 ОС: Ubuntu Linux
🎮 GPU: NVIDIA RTX 3060
📚 OpenCL: 3.0
📊 FFT: clFFT
🔮 Планы: AMD AI100 + ROCm + дополнительные библиотеки
🪟 Позже: Windows (настроим потом)
```

---

## 📋 Что изменилось в CMakePresets.json

### Было (5 конфигураций):
```
❌ linux-rocm-opencl
❌ linux-rocm-debug
❌ linux-cuda
❌ windows-cuda
❌ windows-opencl
```

### Стало (3 конфигурации):
```
✅ linux-nvidia-opencl       (ваша текущая)
✅ linux-nvidia-debug         (для отладки)
✅ linux-amd-opencl          (для AMD AI100, когда будет готово)
```

---

## 🔧 Как использовать

### 1️⃣ Замените файл:
```bash
# Скопируйте новый файл
cp CMakePresets-SIMPLIFIED.json CMakePresets.json
```

### 2️⃣ Конфигурация для RTX 3060:
```bash
# Release build
cmake --preset linux-nvidia-opencl

# Debug build
cmake --preset linux-nvidia-debug
```

### 3️⃣ Сборка:
```bash
# Release
cmake --build build/linux-nvidia -j8

# Debug
cmake --build build/linux-nvidia-debug -j4
```

---

## 📝 Для будущей AMD AI100 конфигурации

Когда вы будете готовы тестировать AMD AI100:

```bash
# Используйте заготовку
cmake --preset linux-amd-opencl

# При необходимости добавим:
# - ROCm пути
# - Дополнительные библиотеки (roc-libraries, hip и т.д.)
# - Оптимизации для AI100
```

---

## 🪟 Windows конфигурация

Когда будете настраивать Windows, просто дайте мне знать:
- Какую GPU будете использовать?
- CUDA или OpenCL?
- Visual Studio версия?

И я добавлю новые presets!

---

## ✨ Структура сборочных папок после первой конфигурации:

```
build/
├── linux-nvidia/          ← Release для RTX 3060
├── linux-nvidia-debug/    ← Debug для RTX 3060
└── linux-amd/             ← Заготовка для AMD AI100
```

---

## 🎯 Следующие шаги:

1. ✅ Замените CMakePresets.json на упрощенный вариант
2. ✅ Конфигурируйте: `cmake --preset linux-nvidia-opencl`
3. ✅ Собирайте: `cmake --build build/linux-nvidia -j8`
4. ⏳ Когда AMD будет готова - добавим конфигурацию
5. ⏳ Когда Windows будет готова - добавим конфигурацию

---

## 💡 Остальные файлы CMake остаются теми же!

- CMakeLists.txt → не меняется
- cmake/*.cmake модули → не меняются
- src/*/CMakeLists.txt → не меняются

**Только CMakePresets.json упрощен под ваши текущие нужды!**

---

**Готово! Используйте CMakePresets-SIMPLIFIED.json!** 🚀
