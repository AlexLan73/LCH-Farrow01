# 🔍 ANALYSIS_AND_PLAN.md

## АРХИТЕКТУРА И ТЕХНИЧЕСКОЕ ОБОСНОВАНИЕ

### ТЕКУЩАЯ АРХИТЕКТУРА

```
OpenCLCore (контекст)
  ↓
CommandQueuePool (очереди)
  ↓
OpenCLComputeEngine (фасад)
  ↓
GeneratorGPU
  ├─ kernel_lfm_basic() → БЕЗ задержек
  ├─ kernel_lfm_delayed() → по УГЛАМ
  └─ kernel_lfm_combined() ← НОВОЕ!
```

### СУЩЕСТВУЮЩИЕ ФУНКЦИИ

**signal_base()** - базовый ЛЧМ
- φ(t) = 2π(f_start·t + 0.5·chirp_rate·t²)

**signal_valedation()** - с угловой задержкой
- delay_angle = (angle_rad × wavelength) / c

### НОВАЯ ФУНКЦИЯ: signal_combined_delays()

**Комбинирует:**
- Угловую задержку (delay_degrees)
- Временную задержку (delay_time_ns)

**В одном kernel'е:**
```
τ_total = τ_angle + τ_time
φ(t) = 2π(f_start·(t-τ) + 0.5·chirp_rate·(t-τ)²)
```

### 9 ШАГОВ ИНТЕГРАЦИИ

1. typedef CombinedDelayParam
2. Члены класса
3. Декларация метода
4. Конструктор
5. Деструктор
6. Move-операции
7. LoadKernels()
8. GetKernelSource()
9. signal_combined_delays()

### ДРОБНАЯ ЗАДЕРЖКА

Поддерживает задержку < шага дискретизации:
- 50 нс → 0.6 отсчётов (интерполяция)
- Работает корректно!
