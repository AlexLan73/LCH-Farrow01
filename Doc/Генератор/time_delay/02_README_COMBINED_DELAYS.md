# 📖 README_COMBINED_DELAYS.md

## ✅ ПОЛНЫЙ ГАЙД: signal_combined_delays()

### ЧТО ЭТО?
Новая функция для генерации ЛЧМ сигналов с **комбинированной задержкой** (угол + время).

### КАК ИСПОЛЬЗОВАТЬ?

```cpp
// Структура параметров
typedef struct {
    uint beam_index;
    float delay_degrees;      // Угловая задержка (градусы)
    float delay_time_ns;      // Временная задержка (наносекунды)
} CombinedDelayParam;

// Подготовить массив
std::vector<CombinedDelayParam> delays(256);
for (int i = 0; i < 256; i++) {
    delays[i].delay_degrees = 0.5f * i;
    delays[i].delay_time_ns = 50.0f * i;
}

// Сформировать сигнал
cl_mem signal = gen.signal_combined_delays(delays.data(), delays.size());
```

### ПАРАМЕТРЫ ДЛЯ 12 MHz

| Задержка | Отсчёты | Тип |
|----------|---------|-----|
| 50 нс | 0.6 | Дробная |
| 100 нс | 1.2 | Дробная |
| 200 нс | 2.4 | Дробная |
| 1000 нс | 12 | Целая |

### ОСОБЕННОСТИ

✅ Комбинированная задержка (угол + время)
✅ Дробная задержка (интерполяция)
✅ GPU параллелизм
✅ Production-ready

### БЫСТРЫЙ СТАРТ

→ **IMPLEMENTATION_SUMMARY.md** (9 шагов)
