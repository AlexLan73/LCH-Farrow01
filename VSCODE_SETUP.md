# 🚀 Быстрый старт: VSCode для LCH-Farrow1

## ✅ Что настроено

В директории `.vscode/` созданы конфигурации для:
- ✅ Сборки проекта через CMake (Debug/Release)
- ✅ Отладки с точками останова
- ✅ IntelliSense для C++ с поддержкой OpenCL

## 🎯 Быстрый запуск

### 1. Сборка проекта

**Через VSCode:**
- `Ctrl+Shift+P` → `Tasks: Run Task` → `CMake: Build (Debug)`

**Или через терминал:**
```powershell
cmake --preset windows-main -DCMAKE_BUILD_TYPE=Debug
cmake --build build --config Debug
```

### 2. Запуск отладки

1. Откройте панель отладки: `Ctrl+Shift+D`
2. Выберите: **"Debug LCH-Farrow1 (Windows)"**
3. Нажмите `F5`

### 3. Установка точек останова

- Нажмите `F9` на нужной строке кода
- При запуске отладки выполнение остановится на точке останова

## 📋 Конфигурация

### Исполняемый файл
- Debug: `build/Debug/LCH-Farrow1.exe`
- Release: `build/Release/LCH-Farrow1.exe`

### OpenCL пути
Настроены для:
```
C:\Program Files\NVIDIA GPU Computing Toolkit\CUDA\v13.0\include
```

Если путь другой, обновите:
- `.vscode/settings.json`
- `.vscode/c_cpp_properties.json`

### CMake Preset
Используется: `windows-main` из `CMakePresets.json`

## 🔧 Требования

- Visual Studio 2022 (с C++ компонентами)
- CMake 3.20+
- OpenCL SDK (NVIDIA CUDA Toolkit)
- Расширение VSCode: C/C++ (Microsoft)

## 📖 Подробная документация

См. `.vscode/README_VSCODE_SETUP.md` для полной инструкции.

---

**Готово к использованию!** 🎉

