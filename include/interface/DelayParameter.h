#pragma once
#include <cstdint>

// ═════════════════════════════════════════════════════════════════════
// DELAY PARAMETER STRUCTURE
// ═════════════════════════════════════════════════════════════════════

/**
 * @brief Параметр задержки для луча
 * Используется в signal_valedation() для расчета сигналов с задержками
 */
struct DelayParameter {
    uint32_t beam_index;        // Номер луча (0-255)
    float delay_degrees;        // Задержка в градусах (0.5, 1.5, 6.0, ...)
};
