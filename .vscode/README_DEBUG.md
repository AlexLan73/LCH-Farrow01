# 🔧 Настройка отладки в VSCode

## ✅ Что настроено

1. **Конфигурация отладки** (`launch.json`):
   - Основная конфигурация: `Debug LCH-Farrow (Linux)`
   - Использует GDB для отладки
   - Автоматическая сборка перед запуском
   - Pretty-printing включен
   - Рабочая директория: `${workspaceFolder}`

2. **Задача сборки** (`tasks.json`):
   - Задача `build (Linux)` для автоматической сборки
   - Использует все доступные ядра CPU (`-j$(nproc)`)
   - Собирает в Debug режиме

3. **Настройки проекта** (`settings.json`):
   - CMake build directory: `${workspaceFolder}/build`
   - Debug режим по умолчанию
   - OpenCL включен

## 🚀 Как использовать

### Запуск отладки

1. **Через F5**:
   - Нажмите `F5` или выберите `Run > Start Debugging`
   - Выберите конфигурацию `Debug LCH-Farrow (Linux)`

2. **Через панель отладки**:
   - Откройте панель отладки (`Ctrl+Shift+D`)
   - Выберите `Debug LCH-Farrow (Linux)` из выпадающего списка
   - Нажмите зеленую кнопку запуска

### Установка точек останова

- **Обычная точка останова**: Кликните слева от номера строки
- **Условная точка останова**: Правый клик → `Add Conditional Breakpoint`
- **Логическая точка останова**: Правый клик → `Add Logpoint`

### Горячие клавиши

- `F5` - Запустить/Продолжить отладку
- `F9` - Установить/Убрать точку останова
- `F10` - Step Over (перешагнуть)
- `F11` - Step Into (войти в функцию)
- `Shift+F11` - Step Out (выйти из функции)
- `Shift+F5` - Остановить отладку
- `Ctrl+Shift+F5` - Перезапустить отладку

## 🔍 Отладка GPU кода

Для отладки OpenCL кода:
- Используйте `printf` в OpenCL kernel'ах
- Проверяйте коды ошибок OpenCL (`cl_int err`)
- Используйте `clGetEventProfilingInfo` для профилирования

## ⚙️ Настройки GDB

В конфигурации включены:
- Pretty-printing для красивого вывода структур
- Intel синтаксис для дизассемблера
- Показ исходного кода в дизассемблере

## 🐛 Решение проблем

### Ошибка: "Unable to start debugging"
1. Убедитесь, что проект собран: `cmake --build build`
2. Проверьте, что исполняемый файл существует: `ls -lh build/LCH-Farrow`
3. Проверьте права доступа: `chmod +x build/LCH-Farrow`

### Ошибка: "No debugging symbols found"
1. Пересоберите проект в Debug режиме:
   ```bash
   cmake -B build -DCMAKE_BUILD_TYPE=Debug -DENABLE_OPENCL=ON
   cmake --build build -j$(nproc)
   ```

### GDB не запускается
1. Проверьте установку: `which gdb`
2. Установите GDB: `sudo apt install gdb`

### Точки останова не срабатывают
1. Убедитесь, что код оптимизирован с `-O0` (в Debug режиме)
2. Проверьте, что файл собран с debug информацией: `file build/LCH-Farrow | grep debug_info`

## 📝 Примечания

- Рабочая директория: `${workspaceFolder}` (корень проекта)
- Все относительные пути в коде должны быть относительно корня проекта
- Для отладки GPU кода используйте профилирование через OpenCL Events

---
*Последнее обновление: 2026-01-12*

