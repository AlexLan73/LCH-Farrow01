# 🚀 Настройка VSCode для проекта LCH-Farrow1

## 📋 Обзор

Эта директория содержит конфигурацию VSCode для запуска и отладки проекта на Windows.

## 📁 Файлы конфигурации

- **`launch.json`** - Конфигурации отладки (Debug/Release)
- **`tasks.json`** - Задачи сборки через CMake
- **`settings.json`** - Настройки рабочей области
- **`c_cpp_properties.json`** - Настройки IntelliSense для C++

## 🔧 Требования

1. **Visual Studio 2022** (Community/Professional/Enterprise)
   - С компонентами: "Desktop development with C++"
   - MSVC компилятор

2. **CMake** (версия 3.20+)
   - Должен быть в PATH

3. **OpenCL SDK**
   - Путь к заголовкам: `C:\Program Files\NVIDIA GPU Computing Toolkit\CUDA\v13.0\include`
   - Если путь другой, обновите в `settings.json` и `c_cpp_properties.json`

4. **Расширения VSCode** (рекомендуемые):
   - C/C++ (Microsoft)
   - CMake Tools (Microsoft)
   - CMake (twxs)

## 🏗️ Сборка проекта

### Способ 1: Через панель задач VSCode

1. Нажмите `Ctrl+Shift+P` (или `F1`)
2. Выберите `Tasks: Run Task`
3. Выберите одну из задач:
   - **CMake: Build (Debug)** - сборка в Debug режиме
   - **CMake: Build (Release)** - сборка в Release режиме
   - **CMake: Rebuild (Debug)** - полная пересборка Debug
   - **CMake: Rebuild (Release)** - полная пересборка Release
   - **CMake: Clean** - очистка директории build

### Способ 2: Через терминал

```powershell
# Debug сборка
cmake --preset windows-main -DCMAKE_BUILD_TYPE=Debug
cmake --build build --config Debug

# Release сборка
cmake --preset windows-main -DCMAKE_BUILD_TYPE=Release
cmake --build build --config Release
```

### Способ 3: Через скрипт run.bat

```cmd
run.bat de    # Debug
run.bat re    # Release
```

## 🐛 Отладка

### Запуск отладки

1. Откройте панель отладки (`Ctrl+Shift+D`)
2. Выберите конфигурацию:
   - **Debug LCH-Farrow1 (Windows)** - Debug режим с автосборкой
   - **Debug LCH-Farrow1 (Windows - Release)** - Release режим с автосборкой
   - **Debug LCH-Farrow1 (Windows - No Build)** - запуск без сборки
3. Нажмите `F5` или кнопку "Start Debugging"

### Точки останова

- Установите точки останова (`F9`) в нужных местах кода
- При запуске отладки выполнение остановится на точках останова

### Горячие клавиши отладки

- `F5` - Продолжить выполнение
- `F10` - Step Over (следующая строка)
- `F11` - Step Into (войти в функцию)
- `Shift+F11` - Step Out (выйти из функции)
- `Shift+F5` - Остановить отладку

## ⚙️ Настройки

### Путь к OpenCL

Если путь к OpenCL отличается от стандартного, обновите в файлах:

**`.vscode/settings.json`:**
```json
"C_Cpp.default.includePath": [
    "C:/Ваш/Путь/К/OpenCL/include"
]
```

**`.vscode/c_cpp_properties.json`:**
```json
"includePath": [
    "C:/Ваш/Путь/К/OpenCL/include"
]
```

### CMake Preset

Проект использует `CMakePresets.json` с preset `windows-main`:
- Генератор: Visual Studio 17 2022
- Архитектура: x64
- OpenCL: включен
- CUDA: выключен

Для изменения настроек отредактируйте `CMakePresets.json` в корне проекта.

## 📂 Структура сборки

После сборки структура будет следующей:

```
build/
├── Debug/
│   ├── LCH-Farrow1.exe      # Исполняемый файл (Debug)
│   └── LCH-Farrow1.pdb       # Отладочная информация
└── Release/
    └── LCH-Farrow1.exe       # Исполняемый файл (Release)
```

## 🔍 IntelliSense

Для правильной работы IntelliSense:

1. Убедитесь, что `compile_commands.json` создан:
   ```powershell
   cmake --preset windows-main -DCMAKE_EXPORT_COMPILE_COMMANDS=ON
   ```

2. Перезагрузите окно VSCode:
   - `Ctrl+Shift+P` → `Developer: Reload Window`

3. Если IntelliSense не работает:
   - Проверьте пути в `c_cpp_properties.json`
   - Убедитесь, что расширение C/C++ установлено
   - Попробуйте `Ctrl+Shift+P` → `C/C++: Reset IntelliSense Database`

## 🚨 Решение проблем

### Ошибка: "CMake preset not found"

- Убедитесь, что `CMakePresets.json` находится в корне проекта
- Проверьте имя preset: должно быть `windows-main`

### Ошибка: "Cannot find OpenCL headers"

- Проверьте путь в `settings.json` и `c_cpp_properties.json`
- Убедитесь, что OpenCL SDK установлен
- Для NVIDIA GPU: путь обычно `C:\Program Files\NVIDIA GPU Computing Toolkit\CUDA\v*\include`

### Ошибка: "Executable not found"

- Убедитесь, что проект собран
- Проверьте путь в `launch.json`: должен быть `build/Debug/LCH-Farrow1.exe` или `build/Release/LCH-Farrow1.exe`
- Если имя проекта изменилось, обновите `launch.json`

### IntelliSense не работает

1. Проверьте `compile_commands.json` в корне проекта
2. Перезагрузите окно VSCode
3. Проверьте настройки в `c_cpp_properties.json`
4. Убедитесь, что расширение C/C++ установлено и активно

## 📝 Примечания

- Все пути используют формат Windows (`C:/` вместо `C:\`)
- Конфигурация настроена для Visual Studio 2022
- Для Linux используйте соответствующие preset из `CMakePresets.json`
- CMakeLists.txt не изменяется - все настройки через CMakePresets.json

## 🔗 Связанные файлы

- `CMakePresets.json` - CMake конфигурация
- `CMakeLists.txt` - Основной CMake файл (не изменять для Windows)
- `run.bat` - Скрипт сборки для Windows

---

**Версия**: 1.0  
**Дата**: 2026-01-10  
**Платформа**: Windows 10/11

