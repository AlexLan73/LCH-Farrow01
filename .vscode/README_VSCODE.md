# Настройка VS Code для проекта LCH-Farrow

## ✅ Что уже настроено:

1. **`.vscode/c_cpp_properties.json`** - конфигурация IntelliSense
2. **`.vscode/launch.json`** - конфигурации запуска и отладки
3. **`.vscode/tasks.json`** - задачи сборки
4. **`.vscode/settings.json`** - настройки проекта
5. **`compile_commands.json`** - симлинк на build/compile_commands.json

## 🔧 Как перезагрузить IntelliSense в VS Code на Ubuntu:

### Способ 1: Перезапуск VS Code (самый надежный)
```bash
# Закройте и откройте VS Code заново
```

### Способ 2: Через Command Palette
1. Нажмите `Ctrl+Shift+P`
2. Введите: `C/C++: Select a Configuration...`
3. Выберите "Linux" (если не выбрано)
4. Или введите: `C/C++: Edit Configurations (UI)` - откроется редактор конфигурации

### Способ 3: Через Command Palette - Reload
1. Нажмите `Ctrl+Shift+P`
2. Введите: `Developer: Reload Window` (если доступно)
   ИЛИ
3. Введите: `C/C++: Reset IntelliSense Database` (если доступно)

### Способ 4: Удалить кэш вручную
```bash
# Закройте VS Code, затем выполните:
rm -rf ~/.cache/vscode-cpptools
# Откройте VS Code заново
```

## 📋 Проверка настройки:

1. Откройте `src/main.cpp`
2. Наведите курсор на `#include "GPU/opencl_manager.h"`
3. Должно работать автодополнение и не должно быть красных подчеркиваний

## 🛠️ Если не работает:

1. **Проверьте, что `compile_commands.json` существует:**
   ```bash
   ls -la compile_commands.json
   ```

2. **Пересоздайте `compile_commands.json`:**
   ```bash
   cmake --preset linux-main
   ln -sf build/compile_commands.json compile_commands.json
   ```

3. **Проверьте расширения VS Code:**
   - Установлено ли расширение "C/C++" от Microsoft?
   - Установлено ли расширение "CMake Tools" (опционально)?

4. **Проверьте пути в `c_cpp_properties.json`:**
   - Откройте `.vscode/c_cpp_properties.json`
   - Убедитесь, что пути правильные

## 📝 Примечания:

- `compile_commands.json` генерируется автоматически при запуске CMake
- Если изменили `CMakeLists.txt`, перезапустите конфигурацию CMake
- IntelliSense может работать медленнее при первом запуске (индексация)

